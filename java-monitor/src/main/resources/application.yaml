server:
  port: 9090

log-mappings:
  applications: [ ]
  defaultMapping:
    warningKeywords: [ "WARNING", "WARN" ]
    errorKeywords: [ "ERROR" ]

management:
  endpoints:
    web:
      exposure:
        include: [ "health", "info", "prometheus" ]